<Window x:Class="MeshGeneration.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MeshGeneration"
        mc:Ignorable="d"
        Title="Mesh Generation Window" Height="692" Width="929" WindowStartupLocation="CenterScreen" 
        FontFamily="Palatino Linotype" FontSize="17">
    <Window.Resources>

        <!-- Label Style -->
        <Style TargetType="Label">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        <!-- TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="BorderBrush" Value="#888"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Width" Value="220"/>
        </Style>

        <!-- Button Style -->
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="6,2"/>
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#007ACC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="90"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                            CornerRadius="4"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="175"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <GroupBox Header="Mesh Generation" VerticalAlignment="Top"  
                  BorderBrush="Black" BorderThickness="1" Margin="20,10,20,0" Height="146">
            <Grid>
                <Label Content="Quad Mesh Segments:" HorizontalAlignment="Left" Margin="27,30,0,0" VerticalAlignment="Top"/>
                <TextBox x:Name="txtBoxzVerticles" HorizontalAlignment="Left" TextWrapping="Wrap" VerticalAlignment="Top" Width="222" Height="26" Margin="263,30,0,0" TabIndex="1" Text="10" Padding="5,0,0,0"/>

                <Label Content="Triangle Mesh Segments:" HorizontalAlignment="Left" Margin="27,70,0,0" VerticalAlignment="Top"/>
                <TextBox x:Name="txtBoxTriSegments" HorizontalAlignment="Left" TextWrapping="Wrap" VerticalAlignment="Top" Width="222" Height="26" Margin="263,70,0,0" TabIndex="2" Text="15" Padding="5,0,0,0"/>

                <Button x:Name="btnLoad" Content="Load" HorizontalAlignment="Left" Margin="514,70,0,0" VerticalAlignment="Top" Click="btnLoad_Click" Width="78" Height="26" TabIndex="3"/>
            </Grid>
        </GroupBox>

        <Canvas x:Name="MeshCanvas" Visibility="Visible" Margin="0" Grid.Row="1"/>
    </Grid>
</Window>
